# Secrets / local env
.env

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Local dev-only media (not committed)
demo/_dev_*
demo/*.mp4

# Local reference material (third-party code kept for study, not redistributed)
/reference/

# Build output
node_modules/
dist/
dist-demo/
.vite/
*.tgz

# Playwright MCP E2E artifacts (screenshots, console/snapshot dumps)
.playwright-mcp/
/*.png

# Local design / planning docs (not part of the published repo)
docs/
